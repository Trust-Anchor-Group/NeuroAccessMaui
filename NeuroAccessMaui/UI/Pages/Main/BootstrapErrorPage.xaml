<?xml version="1.0" encoding="utf-8" ?>
<base:BaseContentPage x:Name="ThisPage" x:Class="NeuroAccessMaui.Pages.Main.BootstrapErrorPage" x:DataType="viewmodel:BootstrapErrorPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:base="clr-namespace:NeuroAccessMaui.Pages"
	xmlns:controls="clr-namespace:NeuroAccessMaui.UI.Controls"
	xmlns:l="clr-namespace:NeuroAccessMaui.Services.Localization"
	xmlns:viewmodel="clr-namespace:NeuroAccessMaui.Pages.Main">

	<Grid
		Padding="{StaticResource LargeMargins}"
		RowSpacing="{StaticResource LargeSpacing}">
		<ScrollView>
			<VerticalStackLayout
				Spacing="{StaticResource MediumSpacing}">
				<Label
					VerticalOptions="Start"
					FontFamily="HaasGroteskBold"
					Text="{Binding TraceTitle}" />

				<Label
					FontSize="14"
					Text="{Binding TraceText}" />
			</VerticalStackLayout>
		</ScrollView>

		<controls:FilledButton
			VerticalOptions="End"
			Command="{Binding CopyToClipboardCommand}"
			Text="{l:Localize CopyToClipboard}" />
	</Grid>
</base:BaseContentPage>
