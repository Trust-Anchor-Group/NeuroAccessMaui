<?xml version="1.0" encoding="utf-8" ?>
<base:BaseRegistrationView x:Name="ThisPage" x:Class="NeuroAccessMaui.UI.Pages.Registration.Views.ValidateEmailView" x:DataType="viewmodel:ValidateEmailViewModel"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:l="clr-namespace:NeuroAccessMaui.Services.Localization"
	xmlns:mct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
	xmlns:base="clr-namespace:NeuroAccessMaui.UI.Pages.Registration"
	xmlns:controls="clr-namespace:NeuroAccessMaui.UI.Controls"
	xmlns:ui="clr-namespace:NeuroAccessMaui.UI"
	xmlns:viewmodel="clr-namespace:NeuroAccessMaui.UI.Pages.Registration.Views">

	<VerticalStackLayout
		Spacing="{StaticResource LargeSpacing}">
		<VerticalStackLayout
			Spacing="{StaticResource MediumSpacing}">

			<Label
				Style="{StaticResource PageTitleLabelStyle}"
				Text="{l:Localize OnboardingEmailPageTitle}" />

			<Label
				Text="{l:Localize OnboardingEmailPageDetails}" />

			<VerticalStackLayout
				Spacing="{StaticResource SmallSpacing}">

				<Label
					Style="{StaticResource SectionTitleLabelStyle}"
					Text="{l:Localize EmailSectionTitle}" />

				<controls:CompositeEntry x:Name="EmailEntry"
					EntryData="{Binding EmailText}"
					PathData="{x:Static ui:Geometries.EmailIconPath}"
					Style="{StaticResource RegularCompositeEntryStyle}">
					<controls:CompositeEntry.Behaviors>
						<mct:TextValidationBehavior x:Name="EmailValidator"
							Flags="ValidateOnValueChanged"
							IsValid="{Binding EmailIsValid}"
							ValuePropertyName="EntryData"													
							RegexPattern="^$|^[\w\d](\w|\d|[_\.-][\w\d])*@(\w|\d|[\.-][\w\d]+)+$" />
					</controls:CompositeEntry.Behaviors>
				</controls:CompositeEntry>

				<Grid
					HorizontalOptions="Center"
					ColumnDefinitions="auto,auto"
					ColumnSpacing="{StaticResource SmallSpacing}"
					IsVisible="{Binding IsNotValid, Source={x:Reference EmailValidator}}"
					RowDefinitions="auto">

					<Path
						Grid.Column="0"
						HeightRequest="20"
						WidthRequest="20"
						VerticalOptions="Center"
						Aspect="Uniform"
						Data="{x:Static ui:Geometries.InfoCirclePath}"
						Fill="{AppThemeBinding Light={StaticResource InvalidatedEditLight},
											   Dark={StaticResource InvalidatedEditDark}}" />
					<Label
						Grid.Column="1"
						VerticalOptions="Center"
						Text="{Binding LocalizedValidationError}"
						TextColor="{AppThemeBinding Light={StaticResource InvalidatedEditLight},
													Dark={StaticResource InvalidatedEditDark}}" />
				</Grid>

			</VerticalStackLayout>
		</VerticalStackLayout>

		<controls:TextButton
			Command="{Binding SendCodeCommand}"
			LabelData="{Binding LocalizedSendCodeText}"
			Style="{StaticResource FilledTextButtonStyle}" />
	</VerticalStackLayout>
</base:BaseRegistrationView>
