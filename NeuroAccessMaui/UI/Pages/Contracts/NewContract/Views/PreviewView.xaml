<?xml version="1.0" encoding="utf-8" ?>
<views:BaseNewContractView x:Name="ThisPage"
						   x:Class="NeuroAccessMaui.UI.Pages.Contracts.NewContract.Views.PreviewView"
						   x:DataType="base:NewContractViewModel"
						   xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
						   xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
						   xmlns:l="clr-namespace:NeuroAccessMaui.Services.Localization"
						   xmlns:mct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
						   xmlns:ui="clr-namespace:NeuroAccessMaui.UI"
						   xmlns:viewmodel="clr-namespace:NeuroAccessMaui.UI.Pages.Registration.Views"
						   xmlns:controls="clr-namespace:NeuroAccessMaui.UI.Controls"
						   xmlns:views="clr-namespace:NeuroAccessMaui.UI.Pages.Contracts.NewContract.Views"
						   xmlns:base="clr-namespace:NeuroAccessMaui.UI.Pages.Contracts.NewContract">

	<ScrollView>
		<VerticalStackLayout Spacing="12" Padding="{DynamicResource MediumLeftRightBottomMargins}">
			<!-- Settings block: contract visibility and future general settings -->
			<Border Style="{DynamicResource BorderSet}" Margin="0">
				<VerticalStackLayout Spacing="8">
					<Label Text="Settings" Style="{DynamicResource ItemTitleLabel}" />
					<controls:CompositePicker
						LabelText="Visibility"
						Style="{DynamicResource RegularCompositePicker}"
						Placeholder="Select who should have access"
						ItemsSource="{Binding Path=ContractVisibilityItems}"
						SelectedItem="{Binding Path=SelectedContractVisibilityItem}" />
				</VerticalStackLayout>
			</Border>

			<!-- Attachments block: informational/pre-selection placeholder -->
			<Border Style="{DynamicResource BorderSet}" IsVisible="{Binding HasAttachmentRequirements}">
				<VerticalStackLayout Spacing="6">
					<Label Text="Attachments (optional)" Style="{DynamicResource ItemTitleLabel}" />
					<Label Text="You can add or upload attachments after creation on the finalize screen." Style="{DynamicResource ItemDescriptionLabel}" />
				</VerticalStackLayout>
			</Border>

			<!-- Human readable contract preview -->
			<Border Style="{DynamicResource BorderSet}">
				<ContentView Content="{Binding HumanReadableText}" />
			</Border>
		</VerticalStackLayout>
	</ScrollView>
</views:BaseNewContractView>
