<?xml version="1.0" encoding="utf-8" ?>
<controls:TemplatedButton x:Name="ThisPage" x:Class="NeuroAccessMaui.UI.Controls.CameraTextButton"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:mct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
	xmlns:base="clr-namespace:NeuroAccessMaui.UI.Pages"
	xmlns:controls="clr-namespace:NeuroAccessMaui.UI.Controls">

	<Border
		BackgroundColor="#33F5F6F7"
		Padding="{StaticResource ButtonMediumMargins}">
		<Border.StrokeShape>
			<RoundRectangle
				CornerRadius="{StaticResource SmallCornerRadius}" />
		</Border.StrokeShape>

		<Label x:Name="InnerLabel"
			HorizontalOptions="Center"
			VerticalOptions="FillAndExpand"
			Style="{StaticResource BoldItemTitleLabelStyle}"
			Text="{Binding Text, Source={x:Reference ThisPage}}"
			TextColor="{StaticResource PrimaryBackgroundLight}" />

		<VisualStateManager.VisualStateGroups>
			<VisualStateGroupList>
				<VisualStateGroup x:Name="CommonStates">
					<VisualState x:Name="Normal" />
					<VisualState x:Name="Disabled">
						<VisualState.Setters>
							<Setter Property="BackgroundColor" Value="#33F5F6F7" />
							<Setter TargetName="InnerLabel" Property="Label.TextColor" Value="{StaticResource PrimaryBackgroundLight}" />
						</VisualState.Setters>
					</VisualState>
				</VisualStateGroup>
			</VisualStateGroupList>
		</VisualStateManager.VisualStateGroups>
	</Border>
</controls:TemplatedButton>
